# Omni MCP Server Configuration
# Copy this file to .env and update with your Omni instance details

# Server Configuration
# ====================

# Omni server URL (required)
# The URL where your Omni instance is running
OMNI_URL=http://localhost:8069

# Database name (optional)
# If not specified, will be auto-detected from Omni
# OMNI_DB=your-database-name

# Authentication Configuration
# ============================

# Authentication method 1: API key (preferred)
# Generate in Omni: Settings > Users & Companies > Users > API Keys
OMNI_API_KEY=your-api-key-here

# Authentication method 2: Username/password (fallback)
# Only used if OMNI_API_KEY is not provided
# OMNI_USER=your-username
# OMNI_PASSWORD=your-password

# Logging Configuration
# =====================

# Log level (optional)
# Valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
# OMNI_MCP_LOG_LEVEL=INFO

# Performance Configuration
# =========================

# Default pagination limit (optional)
# Number of records to return by default
# OMNI_MCP_DEFAULT_LIMIT=10

# Maximum pagination limit (optional)
# Maximum number of records that can be requested
# OMNI_MCP_MAX_LIMIT=100

# Transport Configuration
# =======================

# Transport type (optional)
# Valid values: stdio (default), streamable-http
# OMNI_MCP_TRANSPORT=stdio

# Server host for HTTP transport (optional)
# Only used when transport is streamable-http
# OMNI_MCP_HOST=localhost

# Server port for HTTP transport (optional)
# Only used when transport is streamable-http
# OMNI_MCP_PORT=8000